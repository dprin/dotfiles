import = ["~/.config/alacritty/colors.toml"]

[shell]
program = "/bin/fish"
args = ["-l", "-c", "tmux new-session -A -s general"]

[window]
padding = { x = 2, y = 0 }
dynamic_padding = true

[font]
size=12
normal = { family="MonoLisa", style="Regular" } 
