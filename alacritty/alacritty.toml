# import = ["~/.config/alacritty/rose-pine.toml"]
# import = ["~/.cache/wal/alacritty-colors.toml"]
import = ["./colors.toml"]

[shell]
program = "/bin/zsh"
args = ["-l", "-c", "wal -R && tmux new-session -A -s general"]

[window]
padding = { x = 2, y = 0 }
dynamic_padding = true

[font]
size=12
normal = { family="MonoLisa", style="Regular" } 
